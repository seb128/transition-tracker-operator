summary: Test integration between transition-tracker and haproxy
systems:
  - ubuntu-24.04

execute: |
  pushd "$SPREAD_PATH"

  args="tests/integration/test_ingress.py"

  if [[ -f "$PWD/ubuntu-transition-tracker_amd64.charm" ]]; then
    args="--charm-path=$PWD/ubuntu-transition-tracker_amd64.charm $args"
  fi

  make integration ARGS="$args"

restore: |
  if [[ -z "${CI:-}" ]]; then
    juju destroy-model --no-prompt --destroy-storage testing
    juju add-model testing
  fi
